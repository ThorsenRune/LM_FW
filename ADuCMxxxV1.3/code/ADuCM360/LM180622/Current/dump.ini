FUNC void dump(void) { //No way to pass an array
  int idx;
  exec("log > savedarray.log");
	printf ("xData  \n");
  for (idx = 0; idx < 110; idx++) {
    printf ("%02d \n", xData[idx]);

  }
  exec("log off");
}
FUNC void dump1(void) { //No way to pass an array
  int idx;
  exec("log > savedarray.log");
	printf ("xData1  \n");
  for (idx = 0; idx < 110; idx++) {
    printf ("%02d \n", xData1[idx]);

  }
  exec("log off");
}